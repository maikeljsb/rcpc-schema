{
  "$defs": {
    "CapabilityType": {
      "additionalProperties": false,
      "description": "Something a robot can do, named by a bare verb, that a PrimitiveTask requires and a RobotType offers. The id is the name; matching is by id and there are no levels or qualifiers. Type-level allocation asks whether a robot type offers every capability a task requires.",
      "properties": {
        "description": {
          "description": "What this is, for a human reader.",
          "type": "string"
        },
        "id": {
          "description": "Identifier, unique among instances of its class. The class is always known from the slot that references it, so the id carries no namespace.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "description"
      ],
      "title": "CapabilityType",
      "type": "object"
    }
  },
  "$id": "https://rcpc.for5672/schema/common",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": true,
  "metamodel_version": "1.11.0",
  "title": "common",
  "type": "object",
  "version": "0.1.0"
}
