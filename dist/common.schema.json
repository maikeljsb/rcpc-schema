{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rcpc.for5672/schema/common",
  "metamodel_version": "1.11.0",
  "version": "0.1.0",
  "title": "common",
  "type": "object",
  "additionalProperties": true,
  "$defs": {
    "ComponentPermanence": {
      "type": "string",
      "description": "Whether the component stays in the building. Reinforcement is permanent, formwork temporary.",
      "enum": [
        "permanent",
        "temporary"
      ],
      "title": "ComponentPermanence"
    },
    "Position": {
      "type": "object",
      "additionalProperties": false,
      "description": "Three cartesian coordinates and their unit: a point, or an extent along each axis.",
      "properties": {
        "x_coord": {
          "type": "number",
          "description": "Cartesian x coordinate."
        },
        "y_coord": {
          "type": "number",
          "description": "Cartesian y coordinate."
        },
        "z_coord": {
          "type": "number",
          "description": "Cartesian z coordinate."
        },
        "unit": {
          "type": "string",
          "description": "The unit of the value, of the coordinates, or of the bounds."
        }
      },
      "required": [
        "x_coord",
        "y_coord",
        "z_coord",
        "unit"
      ],
      "title": "Position"
    },
    "Quantity": {
      "type": "object",
      "additionalProperties": false,
      "description": "A number with a unit.",
      "properties": {
        "value": {
          "type": "number",
          "description": "The numeric value."
        },
        "unit": {
          "type": "string",
          "description": "The unit of the value, of the coordinates, or of the bounds."
        }
      },
      "required": [
        "value",
        "unit"
      ],
      "title": "Quantity"
    },
    "Interval": {
      "type": "object",
      "additionalProperties": false,
      "description": "A lower and an upper bound with their unit.",
      "properties": {
        "minimum": {
          "type": "number",
          "description": "The lower bound."
        },
        "maximum": {
          "type": "number",
          "description": "The upper bound."
        },
        "unit": {
          "type": "string",
          "description": "The unit of the value, of the coordinates, or of the bounds."
        }
      },
      "required": [
        "minimum",
        "maximum",
        "unit"
      ],
      "title": "Interval"
    },
    "CapabilityType": {
      "type": "object",
      "additionalProperties": false,
      "description": "Something a robot can do, named by a verb. Required by primitive tasks and offered by robots.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Identifier, unique among instances of its class."
        },
        "description": {
          "type": "string",
          "description": "What this is, in one or two plain sentences."
        }
      },
      "required": [
        "id",
        "description"
      ],
      "title": "CapabilityType"
    },
    "MaterialCategory": {
      "type": "object",
      "additionalProperties": false,
      "description": "A category of material a construction method is written for, IFC's IfcMaterial.Category as this project's vocabulary. Named by a product Material's category and by a method's applies_to.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Identifier, unique among instances of its class."
        },
        "description": {
          "type": "string",
          "description": "What this is, in one or two plain sentences."
        }
      },
      "required": [
        "id",
        "description"
      ],
      "title": "MaterialCategory"
    }
  }
}
